{
  "name": "claude-muse",
  "version": "0.1.0",
  "type": "module",
  "bin": {
    "claude-muse": "./dist/cli.js"
  },
  "scripts": {
    "test": "vitest run",
    "build": "tsc",
    "test:api": "vitest run test/api",
    "test:broker": "vitest run test/broker",
    "test:protocol": "vitest run test/protocol",
    "test:mcp": "vitest run test/mcp",
    "test:e2e": "vitest run test/e2e"
  },
  "dependencies": {
    "sql.js": "^1.11.0",
    "commander": "^13.0.0"
  },
  "devDependencies": {
    "@types/sql.js": "^1.4.9",
    "@types/node": "^22.0.0",
    "typescript": "^5.7.0",
    "vitest": "^2.0.0"
  },
  "engines": {
    "node": ">=26.0.0"
  }
}
